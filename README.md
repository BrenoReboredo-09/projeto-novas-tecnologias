para executar a aplicação realize esses quatro comandos no prompt de comando direto na pasta:

pip install -r requirements.txt

python manage.py makemigrations camara_app

python manage.py migrate

python manage.py runserver
